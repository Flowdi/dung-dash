{
  "name": "dung-dash",
  "version": "1.0.0",
  "type": "module",
  "main": "main.js",
  "scripts": {
    "build": "node build.mjs",
    "check": "npm run build && node scripts/check.mjs",
    "test": "npm run check && node --test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
